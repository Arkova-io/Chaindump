# Migrations are executable database history, but the seeded research records
# intentionally share a declarative shape (the same columns and JSON keys for
# each dossier). Treating that repetition as source-code duplication makes the
# quality gate reject citation data rather than code. Keep all application and
# test files under CPD; exclude migration history only.
sonar.cpd.exclusions=migrations/**
